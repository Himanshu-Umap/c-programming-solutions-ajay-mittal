/*
Program 10 | Input a string and count the number of occurrences of a particular string in the string
*/

#include <stdio.h>

int main(){
    char str[400], srh[400];
    printf("Enter the string: ");
    gets(str);
    printf("Enter the string to search: ");
    gets(str);
    while(str[i] != '\0'){
        while()
    }
}