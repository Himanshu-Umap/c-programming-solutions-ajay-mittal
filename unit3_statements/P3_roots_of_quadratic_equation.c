// Program 3 | Calculate the roots of a quadratic equation

#include <stdio.h>

void main(){
    float a, b, c;
    //for solution find discriminant
    // Quadratic equation: ax^2 + bx + c = 0
    printf("In Quadratic equation: ax^2 + bx + c = 0 enter:\na = ");
    scanf("%f", &a);
    printf("b = ");
    scanf("%f", &b);
    printf("c = ");
    scanf("%f", &c);
}